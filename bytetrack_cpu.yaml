# Lightweight tracker configuration for CPU and static/near-static cameras.
# The detector usually dominates CPU time; ByteTrack adds little overhead.
tracker_type: bytetrack
track_high_thresh: 0.25
track_low_thresh: 0.10
new_track_thresh: 0.25
track_buffer: 45
match_thresh: 0.80
fuse_score: true
